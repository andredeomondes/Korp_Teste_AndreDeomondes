server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Aplicação de página única: qualquer rota desconhecida devolve o index,
    # deixando o roteamento para o Angular.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
